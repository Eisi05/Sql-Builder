package de.eisi05.sql.interfaces;

/**
 * A marker interface indicating that a database statement yields a result set (typically a {@code SELECT} query) upon execution.
 */
public interface ExecuteQueryStatement
{
}
